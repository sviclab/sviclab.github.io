'use client';
import {useMemo,useState} from 'react';
type Publication={id:string;year:number|null;title:string;journal:string;publisher:string;index:string;issn:string;funder:string;doi:string;link:string;linkLabel:string};
export default function PublicationExplorer({publications}:{publications:Publication[]}){const[query,setQuery]=useState('');const[index,setIndex]=useState('all');const shown=useMemo(()=>publications.filter(p=>(index==='all'||p.index===index)&&(`${p.title} ${p.journal} ${p.year}`.toLowerCase().includes(query.toLowerCase()))),[publications,query,index]);return <><div className="toolbar"><input aria-label="Search publications" placeholder="Search title, journal, or year" value={query} onChange={e=>setQuery(e.target.value)}/><select aria-label="Index" value={index} onChange={e=>setIndex(e.target.value)}><option value="all">All indexes</option><option value="SCI(E)">SCI(E)</option><option value="SCOPUS">SCOPUS</option><option value="KCI">KCI</option><option value="International">International</option></select></div><div className="record-list">{shown.map(p=><article className="record" key={p.id}><div className="record-side">{p.year}</div><div><div className="badges"><span className={`badge ${p.index==='SCI(E)'?'sci':p.index.toLowerCase()}`}>{p.index}</span></div><h3>{p.title}</h3><p className="record-meta">{p.journal}{p.publisher&&` · ${p.publisher}`}{p.issn&&` · ISSN ${p.issn}`}</p>{p.funder&&<p className="record-meta">Research support · {p.funder}</p>}<a className="doi-link" href={p.link} target="_blank" rel="noreferrer">{p.doi?`DOI ${p.doi}`:p.index==='KCI'?'Official KCI record · no DOI registered':'Article record · no DOI registered'} ↗</a></div></article>)}{shown.length===0&&<p className="empty">No records found.</p>}</div></>}
