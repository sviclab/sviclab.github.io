import type {Metadata} from 'next';import data from '../../data/achievements.json';import PageHero from '../components/PageHero';import RecordExplorer from '../components/RecordExplorer';
export const metadata:Metadata={title:'Projects | SViC Lab'};export default function Page(){const groups=[...new Set(data.projects.map(p=>p.group).filter(Boolean))].sort();const records=data.projects.map((p,i)=>({id:`p-${i}`,side:[p.startDate,p.endDate].filter(Boolean).join(' – '),title:p.title,meta:[p.funder,p.agency].filter(Boolean).join(' · '),detail:[p.program,p.projectNumber&&`No. ${p.projectNumber}`].filter(Boolean).join(' · '),group:p.group}));return <><PageHero eyebrow="Research portfolio" title="Projects" description={`국가·공공·민간 연구과제의 연차별 수행 기록 ${data.counts.projects}건을 지원기관과 관리그룹별로 확인할 수 있습니다.`}/><section className="content-section page-width"><div className="section-title"><h2>All project records</h2><p>{records.length} records · National project summaries {data.counts.nationalProjects}</p></div><RecordExplorer records={records} groups={groups} placeholder="과제명, 지원기관, 과제번호 검색"/></section></>}
