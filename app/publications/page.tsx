import type { Metadata } from 'next'; import data from '../../data/achievements.json'; import PageHero from '../components/PageHero'; import PublicationExplorer from '../components/PublicationExplorer';
export const metadata:Metadata={title:'Publications | SViC Lab',description:'SViC Lab 국제·국내 학술지 논문 전체 목록'};
export default function Page(){return <><PageHero eyebrow="Research output" title="Publications" description={`국제학술지 ${data.counts.internationalPublications}편과 국내학술지 ${data.counts.domesticPublications}편을 SCI(E), SCOPUS, KCI로 구분했습니다. DOI가 등록되지 않은 논문은 공식 학술 기록을 연결합니다.`}/><section className="content-section page-width"><div className="section-title"><h2>Journal articles</h2><p>{data.counts.publications} records</p></div><PublicationExplorer publications={data.publications}/><div className="section-title" style={{marginTop:70}}><h2>Theses</h2><p>{data.counts.theses} records</p></div><div className="record-list">{data.theses.map(t=><article className="record" key={t.number}><div className="record-side">{t.year}</div><div><h3>{t.title}</h3><p className="record-meta">Graduate thesis</p></div></article>)}</div></section></>}
