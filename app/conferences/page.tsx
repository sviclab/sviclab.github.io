import type {Metadata} from 'next';import data from '../../data/achievements.json';import PageHero from '../components/PageHero';import RecordExplorer from '../components/RecordExplorer';
export const metadata:Metadata={title:'Conferences | SViC Lab'};export default function Page(){const records=data.conferences.map((c,i)=>({id:`c-${i}`,side:c.date||`No. ${c.number}`,title:c.title||c.conference,meta:[c.conference,c.host,c.venue].filter(Boolean).join(' · '),detail:c.projectNumber?`Project ${c.projectNumber}`:'',group:c.scope==='international'?'International':'Domestic'}));return <><PageHero eyebrow="Conferences" title="Conference Activities" description={`${data.counts.internationalConferences} international and ${data.counts.domesticConferences} domestic conference records.`}/><section className="content-section page-width"><div className="section-title"><h2>All records</h2><p>{records.length} records</p></div><RecordExplorer records={records} groups={['International','Domestic']} placeholder="Search title or conference"/></section></>}
